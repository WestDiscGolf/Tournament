@model IEnumerable<Tournament.ViewModels.TeamViewModel>

@{
    ViewBag.Title = "Teams";
}

<h2>Teams</h2>
<p>
    @Html.ActionLink("Create New", "Create")
</p>

<ul>
    @foreach (var team in Model)
    {
        <li>@Html.ActionLink(team.Name, "Edit", new { area = "admin", id = team.Id }, null)</li>   
    }
</ul>