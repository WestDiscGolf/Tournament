@model IEnumerable<Tournament.ViewModels.MatchViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Matches</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>


@foreach (var match in Model)
{
    Html.RenderPartial("_MatchItem", match);
}