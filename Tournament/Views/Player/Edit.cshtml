@model Tournament.ViewModels.PlayerViewModel

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    
    @Html.Partial("_CreateOrUpdate")
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>